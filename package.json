{
  "name": "lawfirmtemplate.webflow.io",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "mkdir -p public && chmod +x ./node_modules/.bin/tailwindcss",
    "prebuild": "cp -r *.html js css Assets public/",
    "build": "node ./node_modules/tailwindcss/lib/cli.js -i ./src/input.css -o ./public/output.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
